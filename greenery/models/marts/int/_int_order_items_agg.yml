version: 2

models:
  - name: int_order_items_agg
    description: "Aggregated products and quantities per order"
    columns:
      - name: order_id
        tests:
          - not_null
          - unique                    
      - name: num_products
      - name: total_quantity
